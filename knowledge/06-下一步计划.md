# 下一步计划

> 进度总览见 [00-索引与进度.md](00-索引与进度.md)。这份文档只讲**接下来做什么**。

## 一、MVP 闭环（已完成 🎉）

```
注册 → 生成关卡（RAG 检索增强）→ 作答生成报告 → 更新薪资 → 排行榜
```

四个步骤全部跑通，详见 [04](04-langchain4j接入.md)、[05](05-生成关卡模块.md)、[08](08-作答报告模块.md)、[09](09-排行榜模块.md)。

## 二、MVP 之后做的（都已完成）

| 模块 | 文档 |
|------|------|
| 密码 BCrypt 加密改造 | [07](07-密码加密BCrypt改造.md) |
| 用户信息更新 / 修改密码 | [22](22-用户信息与密码修改.md) |
| 答题记录 | [10](10-答题记录模块.md) |
| 人气关卡（含改造与前端接口文档） | [11](11-人气关卡模块.md)、[12](12-人气关卡模块改造.md)、[13](13-人气关卡前端接口文档.md) |
| RAG 三档企业化改造 | [14](14-RAG检索增强出题改造.md) ~ [21](21-来源多样性.md) |

## 三、RAG 三档路线的位置

| 档位 | 状态 | 还剩什么 |
|------|------|---------|
| 一 · 可靠性地基 | ✅ | — |
| 二 · 检索质量 | ✅ | — |
| 三 · 数据规模 | 🚧 | 增量更新、评估集与指标 |

## 四、接下来做什么

### 优先级 1：补数据源（当前最大瓶颈）

**这是唯一「不做就没有收益」的事。** 检索链路上的改造（档位二）已经全部做完，
但实测反复证明：**库里没有的东西，检索技巧再花哨也捞不出来。**

| 方向 | 现状 | 要做什么 |
|------|------|---------|
| Java 后端 | 覆盖良好（Java 基础/JVM/并发/集合/MySQL/Spring + Redis） | 可选：`java/new-features`(17)、`java/io`(4)、`database/sql`(6)、`message-queue`(6)、`cs-basics/*`(67) |
| 软件测试 | **只有 1 篇** | 找更多测试面试题/教程 |
| 前端 | **零内容**（原数据源已下线，见 [19](19-文档加载器与知识库扩容.md) §十三） | 找新的前端数据源 |

补数据源的做法：把 URL 加进 `application.yml` 的 `rag.web.urls` → `POST /api/rag/rebuild`。

**先用这套流程验一遍可行性再入库**（踩过两次坑）：
1. 抓的是**文章页**不是首页/目录页 —— 清洗后应有 2000 字以上、开头是具体知识点
2. 别用 curl 判断能不能抓 —— 本机 curl 走 schannel，对 Cloudflare 站会误报失败
3. sitemap 里的域名可能已失效，先解析域名

### 优先级 2：档位三剩下的两项

| 项目 | 解决什么 | 大概方向 |
|------|---------|---------|
| 增量更新 | 现在改 1 个页面也要全量重建（2929 段约 2 分钟） | 按来源比对缓存文件，只重灌变化的部分 |
| 评估集 + 指标 | 现在检索好不好全靠人工抽查，无法量化 | 攒一批「查询 → 期望命中的文章」样本，离线算命中率/召回率 |

**评估集值得早做**：这一轮改造里反复出现「改完不知道有没有变好」的困境
（查询改写的收益就是靠人工对比 top-3 来源才看清的）。有指标以后，
后面所有检索改动都能立刻量化。

### 优先级 3：检索侧的锦上添花（不急）

| 项目 | 何时做 |
|------|--------|
| 改进正文清洗 | 接入更多**非 javaguide** 来源时。现在只删语义标签，对用 class 的站点会漏页脚/侧边栏；届时应换「正文密度」启发式，别堆 class 关键词 |
| 限制单篇进候选池的数量 | 真出现「目标查询被单篇淹没」的案例时。注意会影响 rerank 能挑的片段，别过早做 |
| 多条子查询各自精排再合并 | 项目确实需要「一题覆盖多主题」时（当前一次只生成一道题，不需要） |

### 优先级 4：其他完善方向（早期记录，仍然有效）

- 生成关卡改为从登录用户取薪资（目前是前端传 `salary` 参数）
- 报告 / 流式输出（`langchain4j-reactor` 已引入但未用）
- 关卡按方向 / 难度等扩展
