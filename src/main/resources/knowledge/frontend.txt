前端开发

Vue3 响应式原理：用 Proxy 拦截对象属性的读取和写入，读取时做依赖收集，写入时派发更新。相比 Vue2 的 Object.defineProperty，Proxy 可以拦截新增属性和数组索引变化。

虚拟 DOM 与 Diff 算法：用 JavaScript 对象描述真实 DOM 结构，更新时对比新旧虚拟节点，找出最小差异，再批量操作真实 DOM，减少重排重绘。

前端性能优化：图片懒加载、路由懒加载、代码分割、CDN 加速、防抖节流、减少重排重绘、使用 Web Worker 处理耗时任务。
